import { SafeAreaView } from "react-native-safe-area-context";
import { View } from "react-native";

const Remake = (id: string) => {
    return <View>
    
    </View>;
  };
  
  export default Remake;